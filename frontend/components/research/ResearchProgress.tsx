const phases = [["planning", "Planning"], ["search", "Searching"], ["analysis", "Analyzing"], ["verification", "Verifying"], ["synthesis", "Synthesizing"], ["report", "Report"]] as const;
const phaseFor = (type: string) => type.includes("planning") || type.includes("sub_question") ? "planning" : type.includes("search") || type.includes("source_found") ? "search" : type.includes("source_") ? "analysis" : type.includes("verification") ? "verification" : type.includes("synthesis") ? "synthesis" : type.includes("report") || type.includes("completed") ? "report" : "";
export default function ResearchProgress({ eventTypes, status }: { eventTypes: string[]; status: string }) { const current = eventTypes.map(phaseFor).filter(Boolean).at(-1) ?? (status === "queued" ? "planning" : ""); const currentIndex = phases.findIndex(([key]) => key === current); return <section className="progress-card"><div className="section-kicker">Research progress</div><div className="progress-list">{phases.map(([key, label], index) => <div className={`progress-step ${index < currentIndex || status === "completed" ? "done" : ""} ${key === current && status !== "completed" ? "current" : ""}`} key={key}><span className="step-icon">{index < currentIndex || status === "completed" ? "✓" : index === currentIndex ? "•" : ""}</span><span>{label}</span></div>)}</div></section>; }
