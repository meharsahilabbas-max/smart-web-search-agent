import type { ResearchEvent } from "../../types/research";
const agentFor = (type: string) => type.includes("plan") || type.includes("sub_question") ? "Research planner" : type.includes("search") ? "Web research agent" : type.includes("source") ? "Source evaluation" : type.includes("verif") ? "Fact verification" : type.includes("synth") || type.includes("report") ? "Synthesis agent" : "Research engine";
export default function AgentTimeline({ events }: { events: ResearchEvent[] }) { return <section className="panel timeline"><div className="panel-heading"><div><div className="section-kicker">Live activity</div><h2>Agent timeline</h2></div><span className="pulse-label"><span className="live-dot"/> Streaming</span></div>{events.length === 0 ? <p className="muted">Waiting for the research engine to begin…</p> : <div className="event-list">{events.map((event, index) => <div className="event-row" key={`${event.type}-${index}`}><span className="event-line"><span className="event-marker"/></span><div><strong>{agentFor(event.type)}</strong><p>{event.message}</p><time>{new Date(event.receivedAt).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" })}</time></div></div>)}</div>}</section>; }
